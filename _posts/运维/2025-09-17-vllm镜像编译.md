
```bash
DOCKER_BUILDKIT=1 docker build . --network host --no-cache --build-arg max_jobs=512 --build-arg USE_SCCACHE=0 --build-arg GIT_REPO_CHECK=0 --build-arg RUN_WHEEL_CHECK=false --tag vllm-ci:test --target test --progress plain -f docker/Dockerfile
```

